import React, { Component } from "react";

class HeaderChild extends Component {
  render() {
    return (
      <>
        {/* <h1>Child Component</h1>
        <div>name: {this.props.data.name}and address:{this.props.data.address}</div>
        <button onClick={this.props.changeAddress}>Change through Child</button> */}
      </>
    );
 }
}

export default HeaderChild;
